LISTEN=0.0.0.0:9102

# Optional: override instance label
# INSTANCE_NAME=my-host.example.com

# Ignore list: comma-separated or IGNORE_ALL
SERVICES_IGNORE_LIST=vmagent,health-exporter

# Optional: only include containers with a label
# INCLUDE_LABEL=monitor=true

# Refresh every N seconds
REFRESH_INTERVAL_SECONDS=5

# How many concurrent docker "show()" calls
MAX_CONCURRENCY=20
